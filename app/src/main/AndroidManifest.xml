<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.lapantallasoftware.collectivecrew">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_logo_cc"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_logo_cc"
        android:supportsRtl="true"
        android:theme="@style/ThemeCollective"
        android:name=".ccapp.application.MyApplication">
        <activity
            android:name=".activity.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/ThemeCollective.NoActionBar">
        </activity>
        <activity
            android:name=".activity.SplashActivity"
            android:label="@string/app_name"
            android:theme="@style/ThemeCollective.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>